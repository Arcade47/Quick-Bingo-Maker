// if not quadratic, then there are potentially "small" and "large" bingos
const grid_h_n = 5;
const grid_v_n = 5;

// list of items
const items = [
    ["test"],
    ["line1", "line2"],
    ["line1", "line2", "line3"]
];

// derived
const n_items = grid_h_n*grid_v_n;